{
  "name": "next-secrets",
  "author": "Ian Hunter",
  "license": "MIT",
  "version": "0.0.3",
  "description": "",
  "keywords": [
    "react"
  ],
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "tsc",
    "dist": "yarn clean && yarn build && yarn dist:ui",
    "dist:ui": "(cd ./ui && yarn clean && yarn build && yarn dist) && cp ui/dist/index.html dist/ui.html"
  },
  "devDependencies": {
    "@types/node": "^16.9.1",
    "tslib": "^2.3.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "crypto-js": "^4.1.1",
    "next": "^11.1.2",
    "redis": "^3.1.2"
  }
}
